# loanswipe-assets

Public assets for LoanSwipe email rendering. Hosted via raw.githubusercontent.com so Gmail's image proxy can fetch them reliably (Supabase Storage was non-deterministic in iOS Gmail).
